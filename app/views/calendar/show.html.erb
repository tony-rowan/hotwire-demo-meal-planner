<div class="w-full">
  <h1 class="font-bold text-4xl mb-8">Calendar</h1>

  <%= turbo_stream_from "calendar" %>

  <div id="dates" class="min-w-full">
    <% @dates.each do |date| %>
      <%= render "date", date: date, meal: @meals[date] %>
    <% end %>
  </div>
</div>
